# A simple comparison script in Shell Script

This script compares the numbers entered as parameters in the command:

```bash
./comparison-script 1 2 3
```

Then the script will return the smallest and largest number.
